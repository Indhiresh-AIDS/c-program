#include <stdio.h>
int main() {
    int i, n;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    printf("Numbers from 1 to %d are: ", n);
    for (i = 1; i <= n; i++) {
        printf("%d ", i);
    }
    return 0;
}
