#include <stdio.h>
int main() {
    int a = 10, b = 20;
    a = a + b; // a becomes 30
    b = a - b; // b becomes 10 (original a)
    a = a - b; // a becomes 20 (original b)
    printf("After swap: a = %d, b = %d", a, b);
    return 0;
}
